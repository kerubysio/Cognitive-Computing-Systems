MATERIA: [[Cognitive Computing Systems (6 CFU)]]
DATA: 19/05/2026
FONTE: 

--- 

![[Pasted image 20260519144418.png]]

La gestione dei dati all'interno di un'applicazione cognitiva si sviluppa attraverso due fasi sequenziali e interconnesse: l'**Acquisizione delle fonti di dati** e la **Creazione e raffinamento del corpora**. La prima fase richiede un bilanciamento strategico tra sorgenti strutturate interne, l'analisi di dati archiviati ma storicamente ignorati definiti "dati nascosti", e l'integrazione di fonti esterne organizzate tramite tassonomie e ontologie di dominio. Questo flusso alimenta direttamente il nucleo della gestione dei dati, dove le informazioni subiscono un processo continuo di preparazione e _ingestion_ in tempo reale per garantirne la leggibilità, la ricercabilità e la comprensibilità. Il patrimonio informativo viene poi ciclicamente espanso e raffinato per massimizzare l'accuratezza del sistema, operando sotto una rigorosa _governance_ per il rispetto dei requisiti di privacy e sicurezza. Un esempio pratico di come un dataset strutturato e preparato venga utilizzato per addestrare modelli avanzati è rappresentato dall'uso del database **MNIST** (composto da cifre scritte a mano) per il testing di **Reti Neurali Convoluzionali (CNN o convnet)**. Queste architetture, nate per applicazioni di _computer vision_ come il riconoscimento di caratteri, di oggetti in immagini/video e la guida autonoma, dimostrano l'importanza di una solida base di dati strutturata, estendendo oggi la loro efficacia anche ad ambiti _non-vision_ quali il _natural language processing_ e i sistemi di raccomandazione.

![[Pasted image 20260519144705.png]]

DOve $tf\_env$ sarebbe l'environement di Anaconda in cui hai installato tensorflow.

La funzione $load\_data$ restituisce una tupla di tuple contenenti campioni ed etichette di training e testing.

![[Pasted image 20260519144841.png]]

All'interno della fase di **Data Exploration**, l'analisi preliminare delle dimensioni del dataset MNIST consente di verificare la struttura dei dati prima dell'addestramento del modello.
L'ispezione tramite l'attributo `.shape` mostra che il training set dei dati d'ingresso (`X_train`) è composto da 60.000 immagini bidimensionali con una risoluzione di 28x28 pixel ciascuna, mentre il rispettivo vettore delle etichette di addestramento (`y_train`) contiene i corrispondenti 60.000 valori target monodimensionali. Parallelamente, il dataset destinato alla valutazione delle prestazioni presenta una struttura analoga ma ridotta: il testing set delle immagini (`X_test`) comprende 10.000 campioni da 28x28 pixel, associati a un vettore di 10.000 etichette di test (`y_test`).
Questa verifica garantisce la corretta consistenza dimensionale tra le caratteristiche estratte e i target attesi, ponendo le basi per la successiva configurazione dei tensori nella rete neurale.

![[Pasted image 20260519145040.png]]

La fase di **Visualizing Digits** definisce le operazioni preliminari necessarie per visualizzare graficamente un campione di 24 immagini del training set di MNIST, consentendo un'ispezione visiva diretta della qualità dei dati prima dell'addestramento.
Il blocco di codice mostra l'importazione delle librerie fondamentali per il data visualization in ambiente Jupyter Notebook: il comando `%matplotlib inline` assicura che i grafici generati vengano renderizzati direttamente all'interno delle celle del notebook, mentre l'istruzione `import matplotlib.pyplot as plt` carica il modulo standard per la creazione e la personalizzazione dei grafici. Parallelamente, viene importata la libreria `seaborn` tramite l'alias `sns`, uno strumento avanzato basato su matplotlib che ottimizza l'estetica dei plot e la gestione dei layout grafici, come suggerito dall'istruzione commentata `# sns.set(font_scale=2)`, predisposta per raddoppiare la scala dei caratteri e migliorare la leggibilità dei testi e delle etichette degli assi durante la visualizzazione delle cifre.

![[Pasted image 20260519145333.png]]
![[Pasted image 20260519145643.png|395]]
All'interno della sottofase di **Visualizing Digits**, lo script implementa la logica per estrarre e visualizzare graficamente un campione casuale di 24 immagini dal dataset MNIST, evidenziando empiricamente la variabilità e la complessità intrinseca nel riconoscimento delle cifre scritte a mano. Operativamente, il codice utilizza la libreria NumPy (`import numpy as np`) e la funzione `np.random.choice` per selezionare, senza ripetizione (`replace=False`), 24 indici casuali compresi nel range del dataset di addestramento (`len(X_train)`). Successivamente, tramite `plt.subplots`, viene istanziata una griglia di visualizzazione organizzata in 4 righe e 6 colonne. Un ciclo `for` itera in parallelo (sfruttando la funzione `zip`) sui singoli sotto-grafici appiattiti in un array monodimensionale (`axes.ravel()`), sulle matrici delle immagini estratte (`X_train[index]`) e sulle relative etichette target (`y_train[index]`).
Per ogni iterazione, la singola immagine viene renderizzata in scala di grigi invertita (`cmap=plt.cm.gray_r`) mediante la funzione `imshow`, e la cifra numerica corretta viene impostata come titolo del riquadro (`set_title(target)`). Infine, l'istruzione `plt.tight_layout()` ottimizza automaticamente la spaziatura tra i sotto-grafici per evitare sovrapposizioni del testo, mostrando a schermo la forte eterogeneità di stile, spessore e inclinazione con cui diversi utenti umani scrivono lo stesso numero.

![[Pasted image 20260519150044.png]]

Sotto la fase di **Data Preparation**, la sezione dedicata al **Reshaping the Image Data** descrive i requisiti geometrici fondamentali per convertire le matrici bidimensionali grezze in strutture compatibili con i modelli di deep learning di Keras.
Le reti neurali convoluzionali (_convnets_) sviluppate in questo framework richiedono tassativamente input strutturati come array NumPy, in cui ogni singolo campione deve presentare una forma tridimensionale definita dalla triade `(width, height, channels)`. Poiché il dataset MNIST è composto da immagini in scala di grigi caratterizzate da tonalità comprese tra 0 e 255, ogni pixel possiede un solo canale di informazione cromatica, determinando una forma finale del singolo input pari a `(28, 28, 1)`. Man mano che la rete neurale esegue il processo di apprendimento sui dati somministrati, gli strati convoluzionali generano progressivamente un numero molto più elevato di canali intermedi; questi canali interni non rappresentano più semplici mappe di intensità luminosa, bensì feature via via più complesse e astratte, come bordi, curve e linee, la cui combinazione logica consente alla rete di riconoscere e classificare correttamente le cifre scritte a mano.

![[Pasted image 20260519150325.png]]

Nella prosecuzione della fase di **Reshaping the Image Data**, il codice mostra l'applicazione pratica del metodo `reshape` degli array NumPy per riconfigurare geometricamente le dimensioni dei dataset di addestramento e di test in conformità con i requisiti di Keras. Il metodo riceve come argomento una tupla che definisce la nuova forma tridimensionale estesa per includere il canale singolo della scala di grigi.
Operativamente, l'istruzione `X_train.reshape((60000, 28, 28, 1))` rimodella la struttura originaria delle immagini di addestramento, e la successiva invocazione di `X_train.shape` ne conferma la corretta trasformazione in un tensore a quattro dimensioni composto da 60.000 campioni, ciascuno avente larghezza e altezza di 28 pixel e 1 singolo canale di colore. La medesima procedura viene applicata al dataset di valutazione tramite il comando `X_test.reshape((10000, 28, 28, 1))`, il cui output dimensionale verificato da `X_test.shape` attesta la conversione finale nei 10.000 campioni tridimensionali richiesti per la successiva fase di feeding della rete neurale convoluzionale.

![[Pasted image 20260519150428.png]]
![[Pasted image 20260519150624.png]]
La fase di **Normalizing the Image Data** evidenzia l'importanza di standardizzare i valori numerici delle feature prima di sottoporli a un modello di deep learning, poiché i valori grezzi possono variare su intervalli molto ampi. Le reti neurali profonde garantiscono prestazioni e velocità di convergenza nettamente superiori quando lavorano su dati preventivamente normalizzati all'interno dell'intervallo standard compreso tra 0.0 e 1.0, oppure scalati in un range in cui la media statistica della distribuzione sia pari a 0.0 e la deviazione standard sia uguale a 1.0 (un principio teorico ampiamente documentato nello studio fondamentale sulla _Batch Normalization_ di S. Ioffe e C. Szegedy).
Operativamente, nel contesto del trattamento di immagini digitali a 8 bit come quelle del dataset MNIST, la tecnica più immediata per effettuare questa riscalatura consiste nel dividere matematicamente il valore di intensità di ogni singolo pixel per il valore massimo consentito, ovvero 255, convertendo così l'intervallo originario di interi [0, 255] nel range continuo in virgola mobile [0.0, 1.0] richiesto per l'ottimizzazione dell'addestramento.

![[Pasted image 20260519150648.png]]

La fase di **One-Hot Encoding: Convert Labels to Categorical Data** descrive il processo di trasformazione dei target numerici necessario per consentire a Keras di calcolare correttamente l'accuratezza del modello durante la classificazione multiclasse. Poiché la rete neurale convoluzionale restituisce per ogni cifra un array di 10 probabilità distinte (una per ciascuna classe da 0 a 9), sorge l'esigenza di uniformare la struttura geometrica delle etichette del dataset originale, che inizialmente sono memorizzate come singoli numeri interi compresi tra 0 e 9. Per rendere confrontabili gli output predetti con i target reali, le etichette intere devono essere trasformate in array categorici speculari tramite la tecnica del _one-hot encoding_, la quale converte ogni numero intero in un vettore a 10 elementi composto interamente da valori pari a 0.0, a eccezione dell'indice corrispondente alla cifra rappresentata, che assume il valore di 1.0 (come mostrato nell'esempio in cui il numero 7 viene codificato inserendo un'unità esclusivamente in ottava posizione).
All'interno del framework, questa operazione di preelaborazione categorica viene eseguita in modo diretto richiamando la funzione `to_categorical` contenuta nel modulo `tensorflow.keras.utils`.

![[Pasted image 20260519151052.png]]

La fase di **Creating the Neural Network** definisce l'avvio della configurazione di una rete neurale convoluzionale attraverso l'inizializzazione dell'architettura del modello. Il testo illustra l'utilizzo del modello di tipo **Sequential**, una struttura lineare che permette di impilare i vari livelli (_layers_) della rete affinché vengano eseguiti in modo sequenziale. In questa impostazione classica da rete _feed-forward_, l'output generato da un livello diventa direttamente l'input per lo strato successivo, sebbene venga anticipato che non tutte le tipologie di livelli avanzati trasferiscono necessariamente l'output al livello immediatamente adiacente. Operativamente, il blocco di codice mostra l'importazione di questa componente fondamentale tramite l'istruzione `from tensorflow.keras.models import Sequential` e la successiva creazione dell'istanza della rete, denominata `cnn`, invocando il costruttore vuoto `Sequential()`, che fungerà da contenitore logico all'interno del quale verranno progressivamente aggiunti i successivi strati convoluzionali, di pooling e densi.

![[Pasted image 20260519151207.png]]

La fase di **Adding Layers to the Network** descrive l'architettura strutturale tipica di una rete neurale convoluzionale (_convnet_) e introduce l'importazione delle classi necessarie per definirne i singoli strati. Una struttura _convnet_ standard si compone di tre macro-livelli fondamentali: un _input layer_, il cui compito è ricevere i campioni del dataset di addestramento; una serie di _hidden layers_ (strati nascosti), che estraggono le caratteristiche e apprendono attivamente dai dati somministrati; e infine un _output layer_, deputato a generare le predizioni finali del modello.

![[Pasted image 20260519151353.png]]

La fase dedicata alla **Convolution** descrive il funzionamento logico e operativo del primo strato di estrazione delle caratteristiche all'interno di una rete neurale convoluzionale. Il processo inizia con un _convolution layer_, il quale sfrutta le relazioni spaziali esistenti tra i pixel in stretta prossimità per apprendere feature (o pattern) utili all'interno di piccole aree circoscritte di ciascuna immagine; queste caratteristiche estratte diventeranno poi gli input per i livelli successivi della rete.
Per illustrare il meccanismo matematico in modo semplificato, il testo propone l'esame di una convoluzione applicata a un'immagine campione da 6x6 pixel: un quadrato ombreggiato da 3x3 pixel rappresenta il _kernel_ (o filtro di convoluzione). L'operazione di convoluzione esegue i calcoli analizzando e apprendendo dalle 9 feature elementari racchiuse in quel momento all'interno dell'area del kernel per poi mappare e produrre in output 1 sola nuova feature condensata, che andrà a comporre la feature map finale.

![[Pasted image 20260519151518.png]]

La prosecuzione della spiegazione sulla **Convolution** illustra graficamente il meccanismo di scorrimento spaziale del filtro e la conseguente mappatura dimensionale dei dati dall'input all'output. Il diagramma mostra un'immagine di input con una griglia di 6x6 pixel su cui viene applicato un _kernel_ ombreggiato da 3x3 pixel; l'operazione matematica riassume le 9 feature locali coperte dal filtro per generare un singolo valore numerico nella matrice di output.
Muovendosi passo dopo passo da sinistra verso destra, la prima posizione del kernel mappa il pixel nell'angolo in alto a sinistra della feature map di output (contrassegnato con il numero 1). Successivamente, il filtro trasla orizzontalmente di un pixel alla volta lungo la riga (posizioni 2, 3 e 4), calcolando i rispettivi valori sequenziali della matrice di output. Questo processo di scansione bidimensionale si ripete riga per riga fino a coprire l'intera area d'ingresso, determinando una contrazione geometrica controllata che trasforma la matrice iniziale 6x6 in una feature map finale più densa e concentrata da 4x4 pixel, composta complessivamente da 16 sotto-aree di feature estratte.

![[Pasted image 20260519152120.png]]

La sottofase successiva sulla **Convolution** approfondisce le proprietà geometriche e quantitative dei filtri applicati all'interno di uno strato convoluzionale. Il passaggio completo del kernel sull'intera immagine, muovendosi da sinistra a destra e dall'alto verso il basso, definisce l'azione di un singolo filtro. Dal punto di vista matematico, quando si utilizza un kernel con dimensioni 3x3 senza l'applicazione di un riempimento perimetrale (padding), la matrice di output risultante presenterà una larghezza e un'altezza ridotte di due unità rispetto alle dimensioni originali dell'input; nel caso specifico delle immagini del dataset MNIST da 28x28 pixel, l'estrazione genererà quindi una mappa di feature da 26x26 pixel. All'interno di un livello convoluzionale standard progettato per elaborare immagini di piccole dimensioni, il numero complessivo di filtri applicati contemporaneamente varia solitamente tra 32 e 64, dove ciascun filtro apprende e produce risultati differenti estratti dai medesimi dati d'ingresso. All'aumentare della risoluzione dei campioni grafici cresce proporzionalmente la quantità di dettagli e feature complesse presenti, richiedendo di conseguenza un numero più elevato di filtri integrati; a dimostrazione di ciò, i modelli di reti neurali convoluzionali (_convnets_) preaddestrati dal team di Keras arrivano a impiegare strutture massicce da 64, 128 o persino 256 filtri distinti già all'interno dei loro primi strati convoluzionali.

![[Pasted image 20260519152239.png]]

La sezione successiva sulla **Convolution** descrive la natura degli output generati dagli strati convoluzionali e il principio di astrazione gerarchica delle caratteristiche tipico del deep learning. L'insieme delle risposte prodotte dai singoli filtri all'interno di un livello convoluzionale prende il nome di _feature map_. Gli strati convoluzionali successivi hanno il compito di combinare progressivamente le caratteristiche estratte dalle mappe precedenti, permettendo al modello di riconoscere pattern e feature via via più grandi e complesse; ad esempio, in un sistema di riconoscimento facciale, i primi livelli identificano elementi geometrici semplici come linee, bordi e curve, mentre i livelli successivi aggregano questi dati per riconoscere strutture complesse quali occhi, sopracciglia, nasi, orecchie e bocche. Una volta che la rete ha appreso una determinata caratteristica, diventa in grado di riconoscerla in qualsiasi punto dell'immagine, indipendentemente dalla sua posizione spaziale; questa proprietà di invarianza alla traslazione costituisce uno dei motivi principali per cui le reti neurali convoluzionali (_convnets_) sono ampiamente utilizzate e popolari nel campo dell'object recognition.

Qualche slide prima, abbiamo visto questo snippet di codice utile per dare vari livelli ad una rete convoluzionale:
![[Pasted image 20260519155709.png]]
Il blocco di codice mostra l'importazione delle classi fondamentali necessarie alla costruzione di un'architettura di rete neurale convoluzionale (_convnet_) mediante il modulo `layers` del framework Keras. L'istruzione `from tensorflow.keras.layers import Conv2D, Dense, Flatten, MaxPooling2D` carica nello script i quattro blocchi costruttivi essenziali per i dati d'ingresso.

![[Pasted image 20260519155855.png]]
La fase di **Adding a Convolution Layer** descrive l'aggiunta del primo strato convoluzionale al modello sequenziale e analizza i parametri di configurazione passati al costruttore `Conv2D`. L'istruzione `cnn.add(Conv2D(filters=64, kernel_size=(3, 3), activation='relu', input_shape=(28, 28, 1)))` istanzia lo strato introducendo quattro argomenti fondamentali per l'estrazione delle caratteristiche: il parametro `filters=64` specifica il numero di filtri indipendenti che verranno applicati, determinando lo spessore o la profondità della _feature map_ risultante; il parametro `kernel_size=(3, 3)` stabilisce le dimensioni spaziali (larghezza e altezza) della matrice del kernel utilizzata da ciascim filtro per scansionare l'input; l'argomento `activation='relu'` imposta la funzione di attivazione _Rectified Linear Unit_ per introdurre non linearità nell'output del livello.

La funzione **ReLU** è definita matematicamente come $f(x) = \max(0, x)$, il che significa che lascia invariati i valori positivi e azzera completamente tutti i valori negativi in uscita dalle convoluzioni. Questa operazione di "soglia" è cruciale perché permette alla rete di rompere la linearità del sistema, consentendole di apprendere pattern complessi e non lineari (come curve, contorni e forme geometriche articolate) che una semplice sequenza di moltiplicazioni lineari non potrebbe mai mappare. Come evidenziato nel testo di François Chollet (_Deep Learning with Python_), la ReLU rappresenta la funzione di attivazione più ampiamente utilizzata nel deep learning, comunemente raccomandata per i livelli convoluzionali grazie alle sue ottime prestazioni computazionali: essendo una semplice operazione di massimo, è estremamente facile e veloce da calcolare, e aiuta a prevenire il problema della saturazione del gradiente durante la backpropagation. Infine, il parametro `input_shape=(28, 28, 1)` definisce la geometria tridimensionale dei campioni d'ingresso in termini di larghezza, altezza e canali, un requisito obbligatorio esclusivamente per il primo livello della struttura per consentire alla rete di allocare correttamente i pesi sinaptici iniziali.

![[Pasted image 20260519160029 1.png]]
La prosecuzione della fase di **Adding a Convolution Layer** approfondisce il meccanismo di gestione delle dimensioni geometriche dei dati d'ingresso e il flusso di passaggio delle informazioni tra i livelli adiacenti del modello sequenziale. Quando si configura il primo livello della struttura, è necessario specificare esplicitamente la forma dei campioni d'ingresso tramite il parametro `input_shape=(28, 28, 1)`. Dal punto di vista architetturale, questa istruzione genera implicitamente un _input layer_ fittizio incaricato esclusivamente di caricare i campioni in memoria e di trasmetterli direttamente allo strato `Conv2D`, il quale costituisce a tutti gli effetti il primo vero livello nascosto (_first hidden layer_) della rete. Per tutti i livelli inseriti successivamente all'interno della struttura, Keras è in grado di dedurre e calcolare automaticamente il rispettivo `input_shape` ereditandolo direttamente dalla forma dell'output (_output shape_) generata dal livello immediatamente precedente. Questa proprietà di inferenza dimensionale automatica semplifica notevolmente la progettazione del codice, rendendo estremamente immediato l'accatastamento (_stacking_) in sequenza di numerosi strati convoluzionali, di pooling o densi senza dover calcolare manualmente le contrazioni geometriche dei tensori intermedi.

![[Pasted image 20260519160118.png]]
La sezione dedicata alla **Dimensionality of the First Convolution Layer's Output** analizza l'impatto geometrico e quantitativo che l'estrazione delle caratteristiche esercita sul volume complessivo dei dati manipolati dalla rete. I campioni d'ingresso originari presentano una struttura tridimensionale pari a 28x28x1, traducendosi in un totale iniziale di 784 feature per ogni singola immagine. Avendo configurato il livello convoluzionale con 64 filtri indipendenti e un kernel da 3x3 pixel, la contrazione spaziale riduce la risoluzione planare a 26x26 pixel, ma l'applicazione simultanea dei filtri espande la terza dimensione (la profondità del tensore), generando una _feature map_ complessiva con dimensioni pari a 26x26x64, per un totale finale di 43.264 feature estratte. Questo fenomeno determina un incremento significativo e massiccio della dimensionalità, un volume enormemente superiore rispetto al numero di feature tipicamente elaborate nei modelli di Machine Learning classico. Poiché ogni strato nascosto aggiuntivo estrae e stratifica nuove caratteristiche, la dimensionalità delle mappe tende a crescere in modo rilevante lungo i canali interni; tale proliferazione dei parametri rappresenta una delle ragioni principali per cui i modelli di deep learning richiedono una potenza di calcolo e risorse computazionali estremamente elevate per completare le fasi di addestramento e ottimizzazione.

![[Pasted image 20260519160249.png]]
La fase dedicata all'**Overfitting** analizza una delle problematiche più critiche nell'addestramento delle reti neurali, legata alla complessità del modello e alla gestione delle risorse computazionali. L'overfitting si verifica quando un modello presenta un'architettura eccessivamente complessa rispetto alla natura dei dati che deve mappare, portando la rete, nel caso più estremo, a memorizzare passivamente le feature specifiche del dataset di addestramento anziché apprenderne le regole di generalizzazione. Nel deep learning, questa deriva tende a manifestarsi con frequenza elevata a causa dell'esplosione della dimensionalità dei tensori interni, un fenomeno che non solo inficia le capacità predittive del sistema su dati estranei, ma incrementa in modo esponenziale i tempi di calcolo necessari. Specificamente, quando si esegue il training di modelli di deep learning sfruttando esclusivamente l'architettura hardware delle CPU, i tempi di elaborazione possono dilatarsi fino a diventare intollerabilmente lenti e insostenibili. Per ovviare a queste limitazioni ed evitare il collasso delle prestazioni, si rende necessario introdurre diverse tecniche di regolarizzazione e mitigazione dell'overfitting: all'interno del flusso di progettazione di questa specifica rete neurale convoluzionale, la strategia adottata per ridurre la dimensionalità spaziale e contrastare il fenomeno consiste nell'integrazione di livelli di **pooling**.

![[Pasted image 20260519160556.png]]

La fase di creazione di un **Pooling Layer** descrive l'integrazione dei livelli di sottocampionamento all'interno dell'architettura della rete, definendone gli obiettivi funzionali e i meccanismi di compressione dei dati. Per mitigare il rischio di overfitting e contrarre drasticamente i tempi di calcolo, uno strato convoluzionale viene tipicamente seguito da uno o più livelli deputati a ridurre la dimensionalità spaziale delle feature map uscenti. L'operazione di _pooling_ comprime il volume informativo scartando deliberatamente le caratteristiche meno rilevanti, un processo di astrazione e sintesi che costringe il modello a focalizzarsi solo sulle informazioni salienti, migliorando l'abilità di generalizzazione della rete su dati non ancora visti. La tecnica di sottocampionamento più diffusa e comunemente utilizzata nel deep learning prende il nome di **max pooling**: questa procedura esamina la feature map suddividendola in sotto-aree quadrate da 2x2 pixel, analizza i valori numerici racchiusi in ciascuna di esse e conserva all'interno della matrice di output esclusivamente il valore massimo rilevato, eliminando l'attivazione dei pixel circostanti.

![[Pasted image 20260519160933.png|773]]

Graficamente, l'applicazione numerica e la riduzione geometrica operate dal meccanismo di max pooling su una matrice di campionamento avvengono come mostrato nell'immagine precedente. Il diagramma mostra una feature map di input da 6x6 elementi analizzata attraverso una finestra di pooling da 2x2 pixel (chiamata _pool_). Nella prima posizione, rappresentata dal quadrato azzurro, la finestra racchiude i valori numerici 9, 2, 4 e 5; l'algoritmo estrae esclusivamente il valore massimo, ovvero il 9, mappandolo nella cella corrispondente della matrice di output. Successivamente, muovendosi verso destra con un passo (stride) pari a 2 pixel per evitare la sovrapposizione delle aree di calcolo, la seconda finestra (quadrato arancione) isola i valori 1, 7, 3 e 4, registrando in output il valore massimo pari a 7. La terza iterazione sulla riga esamina i valori restanti e mappa il numero 9. Questo processo di scansione a blocchi disgiunti si ripete simmetricamente per le righe inferiori, dimezzando le coordinate spaziali del tensore: la matrice iniziale da 6x6 pixel subisce così un down-sampling geometrico netto, venendo compressa in una nuova matrice di output da 3x3 pixel che conserva unicamente i picchi di attivazione delle feature estratte.

![[Pasted image 20260519161147.png]]

La sottofase conclusiva di **Adding a Pooling Layer** descrive i vincoli iperparametrici dell'operazione di sottocampionamento e ne formalizza l'implementazione sintattica all'interno del codice. L'algoritmo estrae la caratteristica con il valore massimo da ciascuna finestra isolata (_pool_) garantendo che le aree di scansione (pools) non si sovrappongano tra di loro; per ottenere questo comportamento disgiunto con una finestra tridimensionale da 2x2 elementi, il passo di scorrimento orizzontale e verticale (stride) viene impostato automaticamente a 2.
Poiché ogni gruppo logico di quattro pixel attigui viene sintetizzato e ridotto a un singolo valore numerico, l'applicazione del 2x2 max pooling comprime l'estensione complessiva delle feature planari del 75%. Questo drastico abbattimento geometrico dimezza sia la larghezza che l'altezza del tensore d'ingresso, riducendo le dimensioni della mappa generata dal livello precedente da 26x26x64 a una struttura finale più compatta pari a 13x13x64 elementi, mantenendo del tutto invariata la terza dimensione relativa alla profondità dei canali.

![[Pasted image 20260519161456.png]]

Le architetture convoluzionali complesse prevedono tipicamente l'inserimento in cascata di molteplici strati di convoluzione e pooling; in particolare, i modelli sviluppati dal team di Keras tendono a **raddoppiare** il numero dei filtri nei livelli convoluzionali successivi (passando in questo caso da 64 a `filters=128`) per incrementare la capacità della rete di apprendere relazioni e correlazioni più astratte tra le feature map estratte. Operativamente, l'input che alimenta questo secondo blocco è rappresentato dal tensore tridimensionale da 13x13x64 uscente dal primo livello di max pooling.
L'applicazione dell'istruzione `cnn.add(Conv2D(filters=128, kernel_size=(3, 3), activation='relu'))` riduce la risoluzione spaziale di due unità a causa del kernel 3x3 privo di padding, generando un output intermedio pari a 11x11x128. Successivamente, l'integrazione del secondo livello di sottocampionamento tramite il comando `cnn.add(MaxPooling2D(pool_size=(2, 2)))` deve gestire una matrice a dimensionalità dispari (11x11): i livelli di pooling di Keras risolvono questa asimmetria applicando di default un arrotondamento per difetto (_round down_), escludendo l'ultima riga e l'ultima colonna della griglia per ricondursi a un'area parziale da 10x10 pixel. Di conseguenza, il dimezzamento spaziale operato dalla finestra 2x2 produce un tensore compresso finale pari a 5x5x128 elementi, riducendo significativamente il numero di parametri planari e preservando l'intera profondità dei 128 canali informativi.

![[Pasted image 20260519161842.png]]
La fase di **Flattening** dei risultati descrive il processo che serve per convertire le mappe di caratteristiche multidimensionali nei vettori lineari richiesti dagli strati di classificazione finale. Poiché l'output definitivo del modello deve essere un array monodimensionale composto da 10 probabilità distinte per classificare le cifre da 0 a 9, si rende indispensabile "appiattire" (_flatten_) il tensore tridimensionale uscente dall'ultimo livello di pooling prima di poter calcolare le predizioni. 
Di conseguenza, l'output generato dal livello di _Flatten_ si trasforma in una matrice monodimensionale da 1x3200 feature totali.

![[Pasted image 20260519162116.png]]
La fase di aggiunta di un **Dense Layer** serve per ridurre il numero delle features. Descrive l'integrazione del primo strato completamente connesso volto a elaborare logicamente le caratteristiche estratte e linearizzate per avviarne il processo di classificazione.
Mentre i livelli posizionati a monte dello strato di _Flatten_ avevano il compito esclusivo di scansionare le immagini per apprenderne le feature visive locali (linee, bordi, curve), la rete deve adesso imparare le relazioni globali intercorrenti tra queste caratteristiche per poter determinare quale cifra numerica rappresenti effettivamente ogni immagine d'ingresso.
Questo obiettivo matematico viene conseguito mediante l'inserimento di livelli nascosti di tipo **Dense** completamente connessi (_fully connected_), in cui ogni singolo nodo riceve l'input da tutti i nodi dello strato precedente. Operativamente, l'istruzione `cnn.add(Dense(units=128, activation='relu'))` alloca uno strato composto da 128 neuroni indipendenti (definiti anche _units_), i quali elaborano in parallelo i 3200 output linearizzati provenienti dal livello di srotolamento. Questo passaggio intermedio permette di condensare in modo significativo lo spazio delle feature (da 3200 a 128 dimensioni) e introduce un ulteriore livello di astrazione non lineare grazie all'applicazione della funzione di attivazione ReLU, preparando il flusso informativo per la decisione probabilistica finale.

![[Pasted image 20260519162330.png]]
La presenza di **almeno un livello di tipo Dense** inserito verso la fine della struttura costituisce uno standard per la stragrande maggioranza delle _convnets_, in quanto necessario per mappare i pattern visivi estratti in una decisione finale. Quando le reti neurali vengono progettate per elaborare dataset di immagini decisamente più complessi e a risoluzione molto più elevata rispetto a MNIST — come ad esempio **ImageNet**, che vanta un archivio di oltre 14 milioni di immagini categorizzate — l'architettura richiede una capacità computazionale proporzionalmente superiore. In questi scenari avanzati, i modelli integrano spesso **diversi livelli Dense in cascata**, i quali arrivano a impiegare strutture massicce composte comunemente da **4096 neuroni** per ciascuno strato intermedio. Questa stratificazione profonda e densa è cruciale per combinare le miriadi di combinazioni di feature ad alto livello astratto prima di effettuare la classificazione. Per analizzare l'applicazione pratica di queste configurazioni strutturali su larga scala, il testo suggerisce infine di esaminare direttamente il codice sorgente dei modelli _convnets_ preaddestrati su ImageNet e messi a disposizione all'interno del framework Keras.

![[Pasted image 20260519162357.png]]
L'ultimo livello della struttura è un livello di tipo **Dense** completamente connesso, configurato con un numero di unità esattamente speculare al totale delle classi target presenti nel dataset. Nel contesto del riconoscimento di cifre scritte a mano (MNIST), vengono istanziati **10 neuroni**, dove ciascun nodo rappresenta in modo univoco una classe numerica compresa nell'intervallo tra 0 e 9.
Operativamente, lo strato viene aggiunto tramite l'istruzione `cnn.add(Dense(units=10, activation='softmax'))`. Per produrre il verdetto finale, questo livello utilizza la funzione di attivazione **softmax**. Il compito matematico della softmax è prendere i valori grezzi (chiamati _logits_) uscenti dai 10 neuroni e schiacciarli all'interno di una distribuzione di probabilità che rispetta due vincoli fondamentali:

1. Il valore di output di ciascun neurone è compreso rigorosamente tra 0.0 e 1.0.
    
2. La somma algebrica delle probabilità restituite da tutti e 10 i neuroni è sempre pari a 1.0 (ovvero al 100%).

Grazie a questa normalizzazione esponenziale, il neurone che esprime il **valore di probabilità più elevato** determina la predizione ufficiale del modello per l'immagine analizzata. Ad esempio, se l'ottavo neurone (corrispondente all'indice della cifra 7) restituisce una probabilità del 0.94, la rete indicherà il numero 7 come classificazione finale del campione con un livello di confidenza del 94%.